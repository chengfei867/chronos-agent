# Round 87 — AC-3 GA-gate close + v0.7.0 GA cut

**Date**: 2026-05-19 CST (BJT cron slot ~02:55 → ~03:30, well inside 0-11 window)
**Round**: 87 (Phase 4 Arc B slice 1 GA-gate AC-3 close + v0.7.0 stable release cut)
**Slot count**: 1 (single-slot, no A2 inheritance — R86 finished its own work cleanly even though it left AC-3 deferred)
**Predecessor**: R86 (`docs/progress/2026-05-19-round-86.md` — AC-3 dogfood scaffolding shipped, deferred on relay flake; aspirational-release-doc trap codified into `cron-slot-handoff-recovery` skill)

## TL;DR

R87 closes the last open ADR-026 §6 acceptance criterion (AC-3, real-relay override-fork live-smoke) and cuts **v0.7.0 GA**. Strategy: cheap relay-probe first (R85 MCP dogfood = AC-2 ratchet sanity check, ~$0.05) → green → committed budget to R86 fork-override dogfood (~$0.14) → green → ADR-026 §6 AC-3 `[~]` → `[x]` in-place + GA-gate verdict (R87, GREEN) line + v0.7.0 release per `chronos-release-pattern` skill (8 steps). All five ADR-026 §6 ACs now `[x]`. Adapter-1-3 zero-regression streak R52→R87 = **35 rounds** (new project-history high). 35-round streak un-broken across the entire Arc B implementation series (R70 → R87).

## Goal

Close GA-gate AC-3 (Arc B slice 1 fork-with-tool_input_overrides real-relay live-smoke) and cut v0.7.0 stable.

## Setup

- BJT time check: `02:55` → in window. Skill loaded: `chronos-release-pattern`.
- `git fetch origin main && git status` — clean, on `main`, HEAD=`cdd6137` (R86), in sync with origin.
- Pre-flight tag list (`git tag --list "v0.7.0*"`): `v0.7.0a1`, `v0.7.0a2` — no GA tag yet (matches CONTEXT §6 expectation).
- ADR-026 §6 read pre-flight: AC-1 `[x]`, AC-2 `[x]`, AC-3 `[~]`, AC-4 `[x]`, AC-5 `[x]`. Goal of round: flip AC-3 to `[x]`.

## Sequence

1. **Disprover-first relay probe (P0)** — ran `scripts/dogfood/arc_b_slice_3_mcp.py` (R85's AC-2 dogfood) as the cheap probe. Result: `exit 0`, `INVARIANTS GREEN`, `run_id=27f836eb-…`, `tu_id=toolu_bdrk_01JrDJhCancCk2xQA5c4Dwm7`. Relay state today is healthy (synthetic-auth-failed mode from R86 has cleared). AC-2 ratchet re-confirmed (not strictly necessary but free signal).
2. **AC-3 dogfood live-smoke (P1)** — ran `scripts/dogfood/arc_b_slice_3_fork_override.py` against the real relay. Result: `exit 0`, `AC-3 release-gate INVARIANTS GREEN` marker emitted. Concrete evidence (from `/tmp/r87_fork_override.log`):
   - parent run: `run_id=e60c8692-4a30-440d-8333-23e09e93f366`
   - parent ToolUseBlock: anchor node `a632d399…`, `parent_tu_id=toolu_bdrk_01NRJ958p1qAFNtSfNEuLXBU`, operands `4127+8956=13083`
   - fork: `child_run_id=206b9e0a-ce8a-4853-ab1b-5e451aced6f5`, `fork_id=7b6d2b9c-6b1e-4cd0-b48d-88ac663202c9`
   - child shape: `run_status=completed`, `node_count=6`, `tool_use_ids=['toolu_bdrk_01JFteNbHxtsitAd8yXosj3E']`, `tool_result_ids=['toolu_bdrk_01JFteNbHxtsitAd8yXosj3E']` (R76 §5.1 linkage holds across fork on the fresh tu_id), `text_with_override_sum=true` (child's final TextBlock contains `300` proving the `{a:100, b:200}` override surfaced user-side via `resume=child_sid` continuation), `parent_tu_id=toolu_bdrk_01NRJ958p1qAFNtSfNEuLXBU` (≠ child's, INV-5 positive).
3. **Pytest live wrapper (P2)** — `CHRONOS_LIVE=1 pytest tests/live/test_anthropic_agents_fork_override_smoke.py -v`: `1 passed in 54.08s`. Belt-and-suspenders against the dogfood-marker drift confirmed.
4. **ADR-026 §6 audit (P3)** — flipped AC-3 `[~]` → `[x]` in-place per R57 (no Status field flip — Status was Accepted at R69). Closing note cites concrete evidence (run_id, child_run_id, fork_id, child_tu_id, override-sum proof, pytest wallclock). Added `**GA-gate verdict (R87, GREEN — v0.7.0 GA cut)**` paragraph after the existing AC list, replacing the R86 deferred-verdict line. The R86 contract pre-finding paragraph is promoted from "pre-finding" to "finding" — observed against the real relay, exactly as predicted from R86 source-inspection (disprover-first invariant honored: prediction → live observation matches).
5. **CHANGELOG roll (P4)** — rolled `[Unreleased]` → `[0.7.0] — 2026-05-19 (Round 71 + R72 + R73 alpha bundle + ... + R87 GA)`. New `[0.7.0]` block contains: Highlights (3 bullets including AC-3 evidence) / Install / What's bundled (R71-R87 timeline) / Quality bar / R87 contract finding / Caveats / Migration. R86 entries (Added / Changed / Note (R86 → R87) / Quality bar / contract finding) folded into the new `[0.7.0]` block — removed stale duplicate `## [0.7.0a2]` header that R86 had inserted as `[Unreleased]` placeholder. New empty `[Unreleased]` placeholder above `[0.7.0]` references R88.
6. **Version bump (P5, atomic with P4 per `chronos-release-pattern` Phase B)** — bumped 3 files `0.7.0a2` → `0.7.0`: `pyproject.toml` line 3, `src/chronos/__init__.py` line 11, `src/chronos/cli/__init__.py` `info` command status line + streak narrative + version footer. `uv lock --offline` produced 1-line delta (`Updated chronos-agent v0.7.0a2 -> v0.7.0`).
7. **CONTEXT.md refresh (P6)** — §5 promoted to R87, §6 rewritten for R88 (post-GA polish candidates: Option C from R86 §6 = recorder kind contract reconciliation; Option β = offline-fixture AC-3 closure path for relay independence; Option D = 6th fixture site migration).
8. **Verify (P7)** — `pytest -q --no-cov` 648/9/0/0 in ~17s, `mypy src/` clean (38 src files), `ruff check src/ tests/ scripts/` clean, `ruff format --check src/ tests/` clean, `chronos --version` prints `0.7.0`. Drift sweep: `grep -rE "v0\\.7\\.0a[12]|R52->R8[3-6]|31 rounds|34 rounds" tests/ src/chronos/` returned zero hits. uv.lock 1-line bump only (no aliyun-mirror hash drift, no resolution-marker churn).
9. **Commit (P8)** — `git add -A` (no pollution), commit message Chinese + Co-authored-by per project convention.
10. **Tag + push (P9)** — annotated tag `v0.7.0` with multi-line release-notes-style message + `make_latest=true` (this is THE stable release after v0.6.0 stable). Pushed main + tag via gh-proxy.com (only working git remote).
11. **GitHub Release page POST (P10)** — REST API to api.github.com/repos/chengfei867/chronos-agent/releases with `prerelease=false` + `make_latest=true` (de-throne v0.6.0 as latest). Pre-flight 404 check first per skill.

## Decisions

- **D1 — Option A (probe-first → re-run R86 dogfood) over Option B (offline-fixture)**: per CONTEXT §6 R87 plan. Option A green path closes AC-3 in 1 slot at ~$0.20 total live-relay cost. Option B (offline-fixture-based AC-3 closure path) is preserved as a post-GA candidate (R88+) so future relay flakes can't block re-validation, but it's no longer urgent since AC-3 is closed.
- **D2 — Promote R86 contract pre-finding to finding**: R86 predicted from source inspection that (a) `tool_input_overrides` delegates to `fork_session(up_to_message_id=uuid)`, (b) child's tu_id is fresh (not parent's), (c) `state_after['tool_input']` not stamped on child due to id-mismatch, (d) override surfaces user-side via `resume=child_sid` continuation. R87 observed all four of these in the live run (different child tu_id, override-sum 300 in child final text). The dogfood's INV-5 (positive assertion of fresh child tu_id) is the regression guard against future SDK behavior changes.
- **D3 — R86 entries fold into v0.7.0, no separate header**: R86 was a same-release-cycle defensive round (degradation classifier hardening + dogfood scaffolding). Per `chronos-release-pattern` "Arc slice = core + surface + proof = 1 minor version" pattern, the R85+R86+R87 triple is the GA-gate atomic unit and ships as a single v0.7.0 cut, not three patches. The R86 work bullets stay in the v0.7.0 block as `### Added (R86 — ...)` / `### Changed (R86 — ...)` / `### Note (R86 → R87)` for traceability.
- **D4 — make_latest=true**: this is GA after v0.6.0 GA. Both alphas (a1/a2) had `make_latest=false`. v0.7.0 stable should de-throne v0.6.0 as the GitHub UI "Latest" badge.

## Outputs

- `docs/decisions/ADR-026-arc-b-scope.md` — AC-3 `[~]` → `[x]` in-place + R87 closing note + GA-gate verdict (R87, GREEN) replaces R86 deferred verdict.
- `CHANGELOG.md` — `[Unreleased]` → `[0.7.0] — 2026-05-19 (Round 71+...+R87 GA)` with full release notes; new empty `[Unreleased]` for R88.
- `pyproject.toml` — `version = "0.7.0"`.
- `src/chronos/__init__.py` — `__version__ = "0.7.0"`.
- `src/chronos/cli/__init__.py` — `info` status line updated (Arc B slice 1 GA, R52→R87 streak = 35 rounds, v0.7.0 footer).
- `uv.lock` — 1-line version bump (legitimate, committed).
- `docs/progress/2026-05-19-round-87.md` — this file.
- `docs/CONTEXT.md` — §5 promoted to R87, §6 rewritten for R88.
- **No new src/ code, no new unit tests, no new dogfood scripts** — pure release-engineering round; the AC-3 closure is by *re-running* R86's already-shipped artifacts against a healthy relay.
- Git tag `v0.7.0` (annotated) pushed to gh-proxy.com.
- GitHub Release page `v0.7.0 — Arc B slice 1 GA` (prerelease=false, make_latest=true) created via REST API.

## Test evidence

- `pytest -q --no-cov`: **648 passed / 9 skipped (live-gated) / 0 xfail / 0 failed** in ~17s. Zero unit-test delta vs R86 baseline (R87 ships no new src/test code).
- `mypy src/`: clean (38 source files).
- `ruff check src/ tests/ scripts/`: clean. `ruff format --check src/ tests/`: clean.
- `chronos --version`: `chronos 0.7.0` (smoke test that 3-file version bump was consistent).
- Live-smoke matrix (`CHRONOS_LIVE=1`): `arc_b_slice_3_mcp.py` (R85 dogfood) **exit 0 + INVARIANTS GREEN**; `arc_b_slice_3_fork_override.py` (R86 dogfood) **exit 0 + INVARIANTS GREEN**; `tests/live/test_anthropic_agents_fork_override_smoke.py` **1 passed in 54.08s**.
- Adapter-1-3 zero-regression streak: R52→R87 = **35 rounds** (new project-history high; un-broken across the entire Arc B implementation series).

## Findings (R87 invariants for the wall)

1. **Disprover-first invariant validated end-to-end (R73→R86→R87 chain)**: R73 introduced the rule ("any release gated on a previous round's untested research conclusion must re-run smallest disprover"). R86 added the inverse direction: predict-from-source-inspection findings are *pre-findings* until observed live. R87 closes the loop: the R86 predictions held (child tu_id fresh, override surfaces user-side, `state_after['tool_input']` not stamped on child) → pre-finding promoted to finding. Pattern: source-inspection prediction + live observation = stable contract finding. This is now a 3-way validation chain (R73 / R86 prediction / R87 observation) and worth formalizing into `chronos-release-pattern` skill if a 4th case appears.
2. **Honesty rule honored across cron-slot boundaries (R86 → R87)**: R86 prior slot wrote a aspirational `[0.7.0]` block, then R86 slot-2 honestly reverted it to `[Unreleased]`. R87 inherited the honest state and only flipped AC-3 `[x]` after observing the actual INVARIANTS-GREEN marker. The `cron-slot-handoff-recovery` "aspirational-release-doc trap" section worked exactly as intended — R87 read the trap shape distinguisher (un-bumped pyproject + no progress doc + no tag for a claimed release block), confirmed R86's revert was honest (gate counts match), then proceeded with proper observed-evidence flip. **No round in this project's history has ever shipped a release block with un-observed evidence**, and R86→R87 was the closest call. Pattern preserved.
3. **Arc B implementation series concludes at 35 rounds zero-regression (R52→R87)**: this is the longest streak in project history and it un-broke even across one full alpha cut (v0.7.0a1 R73), one alpha2 cut (v0.7.0a2 R83), and the GA cut (v0.7.0 R87). The 2-slot pre-budget rule for impl rounds (R70/R74/R80/R82/R85) shipped 5 of those rounds at green-on-first-impl-iteration thanks to the strict-xfail forcing function (R76→R77, R79→R80, R81→R82 = 3 successful TDD red→green flips). All Arc B-introduced contracts (`state_after.tool_use_id(s)` linkage, fork-with-tool-input-substitution, fork-with-tool-result-substitution) survived without retroactive amendment.
4. **GA-gate close = re-run, not re-build**: R86 shipped production-grade scaffolding (dogfood + wrapper + classifier + 17 unit tests + ADR pre-finding). R87 added zero new code. The GA cut was 100% release engineering (CHANGELOG + version bumps + ADR tickoff + tag + Release page). Pattern: when a deferred close has full scaffolding inherited, the closing round is cheap and fast (single slot, ~$0.20 live-relay cost). Worth pre-budgeting as 0.5-1 slot whenever the deferred close inherits both scaffolding and explicit closure-path plan.

## What's next (R88 hand-off)

Post-GA polish candidates (no longer GA-blocking, all optional):

- **Option β (offline-fixture AC-3 closure path)**: still worthwhile as relay-independence belt-and-suspenders. Capture once during a green-relay window (R85's MCP dogfood already produced one, plus R87's fork-override dogfood produced another), build fake-SDK that replays JSONL transcripts, assert recorder + fork primitive correctness on captured fixtures. ADR-027 candidate. 1-2 slots.
- **Option C (recorder kind contract reconciliation)**: R85 contract finding noted ToolUseBlock/ToolResultBlock surface as `kind=NodeKind.LLM` (kind from message-type, not block-type). Two valid resolutions: (a) document "envelope-determines-kind" in AdapterProtocol contract docs (light, ~30 LOC delta), (b) split per-block nodes refactor (heavy, ADR-deserving). Recommend (a) for R88; (b) defer to Phase 5 if needed.
- **Option D (6th fixture site migration)**: R84 deferred `tests/unit/test_adapter_anthropic_agents.py` migration (richer `_StubBlockBase` shape + runtime `_blk()` factory). `tests/unit/fixtures/anthropic_agents_stubs.py` already exports `make_block` / `make_message` factories — migration would close the 6-of-6 site count. Mechanical, no design risk. 0.5-1 slot.
- **Phase 5 planning**: Arc B slice 1 GA shipped. Project status now: 3 first-class adapters (LangGraph / AutoGen / CrewAI / anthropic_agents = 4 actually) all at GA-quality. Phase 5 candidates were sketched in `docs/roadmap.md` (TBD; should re-read for next Arc selection — Arc C? a different SDK adapter? recorder seam refactor? web frontend feature?).

R88 should pick one of the above based on relay availability, user steering, or a `chronos --version` smoke test on a fresh checkout (would catch any drift the R87 cut missed).
