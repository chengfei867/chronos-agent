# Round 108 — Phase 6 Track 1 slice 1: CLI help/error 文案 polish

**When**: 2026-05-26 CST cron slot ~07:10 (BJT 0–11 working window).
**Slot shape**: A2 close-out **+** new slice landing in the same slot —
prior cron slot authored R108's CLI polish work (10 modified files,
666/9 gates green) but ran out of budget before commit/push.
This slot ran the standard A2 doc close-out (CHANGELOG already authored
by prior slot, finish with progress doc + CONTEXT §5/§6 + commit + push)
on top of the prior slot's authored deliverables.
**Phase 6 RC track**: 1/14 (R108 of R107-R120 polish & ship arc).

## Context — picked up mid-air (per `cron-slot-handoff-recovery` skill)

The 60-second diagnostic showed:

- `git log origin/main..HEAD` empty (no unpushed commits).
- `git status --short` reported **11 modified files**:
  - `CHANGELOG.md` (`[Unreleased]` body filled in by prior slot
    documenting R108 polish — preserved verbatim).
  - `docs/cli-reference.md` (full rewrite from terse table-style to
    operator-facing reference matching R108 docstrings).
  - `src/chronos/cli/__init__.py` (+138 −0 — rich docstrings on every
    Typer-wrapped verb; `Example::` blocks + `Exit codes:` tables).
  - `src/chronos/cli/{compare,diff,fork,forks,replay,runs,tree,verify_golden}.py`
    (+1–4 LOC each — actionable `[dim]Hint:[/]` lines on every error
    `console.print` site that surfaces a missing run / missing fork /
    unknown `--emit` value).
- `git log -3` showed `7025982 release: v0.9.0 — Phase 5 Arc D close-out
  + Phase 6 RC kickoff (R107)` at HEAD = origin/main (R107 v0.9.0 GA
  shipped cleanly; R108 starts on top).
- No progress doc for R108 yet, no CONTEXT §5/§6 R108 paragraph yet.
- Latest progress doc on disk: `2026-05-23-round-98.md` (R99–R107 are
  reflected in CONTEXT §5 paragraphs but their progress docs were
  consolidated into the v0.9.0 release commit; this is the documented
  shape for release-engineering rounds since R98).

This is the canonical **A2 with green gates pre-existing** shape: the
prior slot authored release-quality work, gates were already green at
the WIP state, ran out of iteration budget before the close-out tail
(progress doc + CONTEXT §5/§6 + git commit/push + QQ report). 16th A2
close-out in the project chain (R48-A → R51 → R52 → R53 → R59 → R63 →
R65 → R67 → R70 → R72 → R88 → R91 → R96 → R102 → R105 → R107(?) →
**R108**). Standard 5-item recipe applied verbatim. The `chronos-release-pattern`
skill was NOT invoked (this is feature-doc work, not release engineering).

## What R108 ships (authored by prior slot, landed by this slot)

### Track A — `--help` docstring polish on all 11 verbs

`src/chronos/cli/__init__.py` Typer command bodies now carry rich
docstrings rendered as `chronos <verb> --help`. Each docstring has:

1. A 1-paragraph **summary** of what the verb does + when to reach for it.
2. An **`Example::`** code block with realistic invocations (truncated
   UUIDs like `7c3f9e2a-...-a91`, multiple flags shown).
3. An **`Exit codes:`** table listing every `typer.Exit(code=N)` site
   the verb can hit, with a one-line operator action per row.

Concretely, the 11 verbs (matching the R120 acceptance checklist):

| Verb | Docstring shape | Exit codes documented |
|---|---|---|
| `info` | summary + example | (no exit codes — read-only) |
| `web` | summary + example | 0 happy / 1 port-or-extra / 2 db-missing |
| `replay` | summary + 3-line example | 0 happy / 1 run-missing / 2 db-missing |
| `runs list` | summary + 4-line example | 0 happy / 2 db-missing |
| `runs show` | summary + 2-line example | 0 happy / 1 run-missing / 2 db-missing |
| `forks show` | summary + 2-line example | 0 happy / 1 fork-missing / 2 db-missing |
| `tree` | summary + 3-line example | 0 happy / 1 run-missing / 2 db-missing |
| `diff` | summary + example (was rich before R108, exit-code table appended) | 0 happy / 1 run-missing / 2 db-missing |
| `compare` | summary + example (was rich before, preserved) | 0 happy / 1 run-missing / 2 db-missing |
| `fork plan` | summary + example | 0 happy / 1 logical-error / 2 unknown-emit |
| `verify-golden` | summary + example (was rich before, preserved) | 0 happy / 1 mismatch / 2 missing / 3 sanitiser |

R120 acceptance row "CLI: 11 个 verb 全部 `--help` 含 example, error 含
actionable hint" — both halves now satisfied.

### Track B — actionable `Hint:` lines on every error stderr

Every `console.print(f"[red]error:[/] no such {run|fork}: ...")` site
now follows up with a dim-styled hint pointing at the operator's next
move. Touched sites (8 files, 11 hint insertions):

- `src/chronos/cli/runs.py` → `chronos runs show` no-such-run
- `src/chronos/cli/forks.py` → `chronos forks show` no-such-fork
- `src/chronos/cli/replay.py` → `chronos replay` no-such-run
- `src/chronos/cli/tree.py` → `chronos tree` no-such-run
- `src/chronos/cli/diff.py` → `chronos diff` no-such-run
- `src/chronos/cli/compare.py` → 3 sites (`compare`, `_run_auto_pivot`,
  `_run_matrix`)
- `src/chronos/cli/fork.py` → `fork plan` no-such-run + unknown-`--emit`
- `src/chronos/cli/verify_golden.py` → no-such-run

Existing test substrings (`"no such run"`, `"no such fork"`) are
preserved — the hint line is appended after the error line, so all
existing assertions keep matching. **Test count unchanged at 666/9-skipped**.

### Track C — new `docs/cli-reference.md`

Full rewrite from the prior terse-table style into an operator-facing
reference document mirroring the new `--help` output. Structure: one
`### <verb>` section per verb, each with a code-block synopsis, the
docstring summary, and the exit-code table. Top of document carries:

- Verb summary table with [in-doc anchor links](#info) for every verb.
- Exit-code conventions table (the 4-row contract: 0/1/2/3) so future
  rounds adding a new verb know which exit codes to reuse vs. introduce.

Cross-reference: `CHANGELOG.md` `[Unreleased]` Added block names
`docs/cli-reference.md` so the v1.0 release notes will link it.

## R108 self-check (mandatory per CONTEXT §5 R120 block)

> **本轮是否仍在 R107-R120 polish 轨道?**
>
> **是.** R108 = Phase 6 Track 1 slice 1 (CLI Polish — help/error 文案)
> per the §5 R120 acceptance table. Zero new feature code, zero ADR
> change, zero schema change, zero adapter touch (streak preserved at
> R52→R108 = 56 rounds). The work directly satisfies two R120 must-pass
> checklist rows: "CLI: 11 个 verb `--help` 含 example, error 含
> actionable hint" and partially satisfies the v1.0 readiness rubric.
> Distance to R120: 12 rounds (R109-R120) ≈ 3 days at 4 slots/day.

## Gates re-verified post-pickup (this slot's contribution)

The prior slot's WIP had one residual `ruff format` drift in
`src/chronos/cli/fork.py` (the new 3-line `console.print(...)` call for
the `--emit` hint exceeded line-length and needed a multi-line reflow).
This slot ran `uv run ruff format src/chronos/cli/fork.py` (1 file
reformatted), then re-ran the full gate suite:

```
uv run pytest -q --no-cov   →  666 passed, 9 skipped (live) in 20.89 s
uv run ruff check src tests →  All checks passed!
uv run ruff format --check  →  113 files already formatted (incl. fork.py)
uv run mypy src             →  Success: no issues found in 42 source files
```

Smoke-tested `chronos --help`, `chronos replay --help`, `chronos runs list --help`
— all render correctly with Example + Exit codes.

`git diff pyproject.toml uv.lock` empty (R65/R68 lockfile-trap not
triggered — no `uv run` changed the lock).

## R108 key decisions (logged for ratchet)

- **D-108-1: Single-commit single-slot for the entire R108 polish.**
  CHANGELOG bullet + 11-verb docstring rewrite + 11 hint-line
  insertions + cli-reference.md rewrite + progress doc + CONTEXT §5/§6
  — all logically one slice ("CLI Polish slice 1: documentation"). One
  commit, prefix `docs(cli):`, mirrors the v0.9.0 single-commit
  precedent (D-107-2).
- **D-108-2: Hint lines after, not replacing, the existing error.**
  All existing tests assert on substrings like `"no such run"` or
  `"no such fork"`. Replacing those phrases with hint-embedded variants
  would have broken ~6 tests and required re-ratcheting (R45-A risk).
  Appending a separate `console.print` after the error keeps the
  contract stable. Tests stayed at 666/9 with zero edits to test
  files.
- **D-108-3: `Hint:` styled with `[dim]` Rich tag.** Operator gets a
  visual hierarchy — bold red error first, dim hint second. Matches
  the existing pattern used in `chronos verify-golden` for "secret
  detected" output. Pipe-friendly (Rich strips colour on non-TTY).
- **D-108-4: `cli-reference.md` is a doc, not auto-generated.** Could
  have wired Typer's `typer-cli` to dump help text to markdown, but
  that would (a) introduce a new dev dep, (b) break the operator
  reading experience because Typer's auto-doc shape is verbose and
  un-grouped, and (c) lose the cross-references between verbs that the
  hand-curated doc has. The doc is short enough (~330 lines) that
  manual maintenance through R120 is cheap; if it grows past R120,
  revisit auto-gen as a post-1.0 dev-experience task.
- **D-108-5: Adapter zero-regression streak preserved.** R108 didn't
  touch `src/chronos/adapters/`. Streak ratchets to **R52→R108 = 56
  rounds** (NEW project-history high, +1 vs R107=55). Target at R120 =
  R52→R120 = 68 rounds.

## R108 outputs

- 1 new file: `docs/progress/2026-05-26-round-108.md` (this file).
- 11 modified files (authored by prior slot, landed by this slot):
  - `CHANGELOG.md` (R108 Added block under `[Unreleased]`).
  - `docs/cli-reference.md` (full rewrite, ~330 lines).
  - `src/chronos/cli/__init__.py` (+138 LOC — docstrings).
  - `src/chronos/cli/{compare.py,diff.py,fork.py,forks.py,replay.py,
    runs.py,tree.py,verify_golden.py}` — hint lines.
- 1 lint-format pass: `src/chronos/cli/fork.py` reformatted (3-line
  hint → multi-line reflow).
- 0 new tests (R108 is documentation polish; existing 666 tests cover
  the surface).
- 0 ADR amendments. 0 schema change. 0 `pyproject.toml`/`uv.lock`
  change. 0 adapter change. 0 frontend change. 0 i18n change. 0 spike.

## Distance to R120

12 rounds remaining (R109, R110, R111, ..., R120). At ~4 slots/day in
the 0–11 BJT window, ≈3 days of work. R109 plan (next): `chronos
quickstart` verb (CLI Polish slice 2). See CONTEXT §6.

## Next round (R109) — preview (CONTEXT §6 rewritten this slot)

R109 implements `chronos quickstart [--demo <name>]` — a one-shot CLI
that scaffolds a demo `chronos.db`, loads `examples/<name>/envelopes.jsonl`
(or a built-in minimal demo run when `--demo` is omitted), and either
opens the web UI or prints next-step instructions. Acceptance: a fresh
`venv` + `uv pip install chronos-agent` + `chronos quickstart` puts
the user into a working session with one recorded run + one fork-child
in <10 seconds. Single-slot, single-commit, with 3–5 unit tests
(default path, `--demo` path, missing-demo error, `chronos.db`-exists
overwrite-confirm path, web-port-busy fallback).

## Phase 6 progress (R107-R120 ledger)

| Round | Track | Status |
|---|---|---|
| R107 | Phase 5 收口 (v0.9.0 GA) | ✅ shipped |
| **R108** | CLI Polish slice 1 (help+errors+ref doc) | ✅ **shipped this slot** |
| R109 | CLI Polish slice 2 (`quickstart`) | next |
| R110 | CLI Polish slice 3 (`doctor`) | queued |
| R111-R114 | 前端 P0 清扫 + Onboarding Tour | queued |
| R115-R117 | README 双语 + GIF + 文档站 + examples/ | queued |
| R118-R120 | E2E + v1.0.0-rc1 + R120 验收 | queued |

R120 acceptance progress: **2/11 rows complete** (CLI help+error rich
docstring row + Adapter zero-regression streak partially on track —
56/68 needed by R120, naturally extends if R109-R119 stay off the
adapter directory).

## Follow-ups for R109 cron

- Read `docs/CONTEXT.md` §5 top R120 acceptance block (mandatory every
  round per the Phase 6 charter).
- Read this progress doc (R108) for what's already shipped on the CLI
  surface.
- Standard cron-slot-handoff-recovery diagnostic at slot start.
- One-deliverable-per-slot rule: don't combine R109 quickstart impl
  with the R110 doctor verb. Keep the slice atomic.
