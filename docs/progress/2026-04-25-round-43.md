# R43 — Phase 3 on-ramp combo (2026-04-25)

**Round:** 43 (combo: R43-B + R43-A + R43-D)
**Outcome:** ✅ All three shipped in one round per R42-A's estimate.
**Time:** ~1 session (≈1 "round" equivalent).
**Headline:** Phase 3 v0.3.0 on-ramp is architecturally locked —
  ADR + guide + schema decision all on main.

## What we did

R42-A had ended with three options on the table: **R43-A** (docs),
**R43-B** (ADR), **R43-C** (reopen ADR-013 — rejected; high cost, low
evidence). User said "继续干活". Per user convention, that means
*"execute the research doc's recommended next steps, don't ask"*. So we
ran **R43-B → R43-A → R43-D-combo**:

1. **R43-B — ADR-019 "Chronos does not sandbox fork execution"**
   (`docs/decisions/ADR-019-chronos-does-not-sandbox.md`, 9.4 KB)
   - Codifies what ADR-013 implied but never wrote down.
   - Three-trigger reopen rule mirrors ADR-013's.
   - Rejects E2B, Modal, subprocess-with-nsjail explicitly.
   - Cross-linked from ADR-013.
2. **R43-A — side-effects user guide**
   (`docs/guides/side-effects.md`, 10.1 KB)
   - Three idiomatic patterns: mock transport / envvar kill-switch /
     pure-effectful tool split.
   - Full Chinese translation (matches project bilingual convention).
   - "Fork before/after side-effect node?" cheat-sheet table up top.
   - Forward-references PH3-02 metadata without promising timelines.
3. **R43-D — PH3-02 effects schema spike + decision**
   (`tests/spikes/spike9_effects_metadata.py` + `docs/research/ph3-02-effects-schema-decision.md`)
   - Spike 9: 3-node LangGraph, retrofit `metadata["effects"]` via SQL
     UPDATE, reopen Store, verify F1 (round-trip) + F2 (no regression)
     + F3 (query in ≤2 lines Python). **All green.**
   - Decision: **Option B — annotation-level via `node.metadata[...]`**.
     No schema migration. No core-model change. PH3-02 adapter heuristic
     + UI badge can ship in ≈1.5 rounds.

## Why it matters

Before this round, the Phase 3 on-ramp had three unknowns:

| Unknown | Now |
|---------|-----|
| Is the sandbox milestone still valid? | ❌ No — ADR-019 closed it |
| Do users have guidance? | ✅ `docs/guides/side-effects.md` |
| How do we ship effect metadata? | ✅ `metadata["effects"]`, zero migration |

v0.3.0 is now **execution-only** work (adapter heuristic + UI badge).
Combined estimate holds at **3.5–4.5 rounds** as R42-A projected.

## Files changed

**Added:**
- `docs/decisions/ADR-019-chronos-does-not-sandbox.md` (9.4 KB)
- `docs/guides/side-effects.md` (10.1 KB)
- `docs/research/ph3-02-effects-schema-decision.md` (5.8 KB)
- `tests/spikes/spike9_effects_metadata.py` (6.0 KB)
- `docs/progress/2026-04-25-round-43.md` (this doc)

**Modified:**
- `docs/decisions/ADR-013-fork-auto-execution-stay-frozen.md` — one-line
  cross-link to ADR-019.

**Green bar:** ruff + ruff format + mypy-src all clean; 386 pass / 2 skip,
94% coverage.

## Guardrails respected

- ✅ `docs/roadmap.md` §Phase 3 bullet **not rewritten** — R42-A already
  added the "under review" callout; ADR-019 is the sign-off document,
  but the actual bullet stays untouched until user explicitly OKs the
  rewrite (charter change = user decision).
- ✅ ADR-006 diff algorithm untouched (FROZEN).
- ✅ ADR-013 fork auto-execution untouched (FROZEN); we only added a
  cross-reference, not a reopen.
- ✅ No version bump — R43 is pure research + docs. v0.3.0 ships after
  PH3-02 implementation.

## Next

PH3-02 implementation: adapter `effect_kind` heuristic + Web UI fork
warning badge. Estimate 1.5 rounds. Optional prerequisite: a synthetic
fixture (0.5 round).

Or: pivot — R43 consumed the strategic debt; next could be a user-facing
feature (e.g. `chronos replay` CLI, v0.2.2 bugfix sweep, etc.).

Waiting on user "继续干活" to pick.
