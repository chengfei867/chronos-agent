# Round 114 — Phase 6 Frontend P0 cleanup slice 2/3 + PID-file port-leak fix

**Date**: 2026-06-03 (BJT 04:48 cron slot)
**Track**: R107-R122 polish road / Phase 6 — frontend P0 cleanup slice 2/3 + port-leak long-term fix
**Phase 6 hard target**: ≥3 P0 fixed + skill discipline + PID file shipped
**轨道自检**: ✅ 仍在 R107-R122 polish + ADR-029/030 轨道 — 本轮专攻 R113 catalogued P0 (F7/F9/F10) + skill 加载守约 + 端口泄漏长期修. 不引入新 ADR / 新方向.

---

## TL;DR

3 frontend P0 fixed (F7 viewport re-fit, F9 MiniMap empty rectangle, F10 NodeDetails Identity-tab Model row), `chronos web` PID-file lifecycle shipped with 4 new tests, port-leak skill updated to reflect "option 1 done". Test baseline 697 passed (was 693 — +4 new). Adapter zero-regression streak now 63 rounds (R52→R114).

## Plan vs reality

R114 plan §6 (locked in 5d256fb) called for:

1. Cron prompt header force-load skill (out of scope for this round — lives in cron config, not repo)
2. Skill "When to use" trigger — already patched in last cron slot's recovery commit; verified content
3. PID-file in chronos/cli/web.py (~30 LOC) — **shipped this round, +50 LOC including helper + cleanup hook**
4. Frontend P0 cleanup slice 2/3 — F7 + F9 + F10 fixed (3 of 5 catalogued P0). F8 (draft-badge overlay) and F11 (LandingPage timing CTA) deferred to R114-cont/R115 — F8 needs more visual investigation, F11 is a 5-min copy tweak that doesn't gate quickstart.

The "Onboarding Tour ship" sub-bullet from the plan was based on the Tour pivot finding (component exists since R36-D). Verifying its current wiring lives in `LandingPage.tsx` would have cost a 6-surface walkthrough this round can't afford post the SIX context compactions of the previous slot. Defer to R115-prologue.

## Files changed

### Frontend (3 files)

- `frontend/src/pages/TreeView.tsx` (+13 LOC)
  - Added `useEffect` watching `baseNodes.length` → `rf.fitView({ padding: 0.15, duration: 200, minZoom: 0.5 })` after a 50ms defer. Fixes **F7** (last node clipped below viewport — ReactFlow's `fitView` prop only fits on initial render, not when data first arrives or "Show full fork tree" toggles add descendants).
  - Conditional MiniMap render: `{rfNodes.length >= 6 && (<MiniMap …/>)}`. Fixes **F9** (3-node graph rendered MiniMap as an empty dark rectangle below the Legend — looked like a broken placeholder. Below 6 nodes the MiniMap adds no nav value, only visual noise).

- `frontend/src/components/NodeDetails.tsx` (+8 LOC)
  - Identity tab now surfaces `Model` row (purple `<Tag>`) when `node.model_name` is set — mirrors the Cost-tab rendering. Fixes **F10** (users investigating a node had to switch tabs to learn what model produced it; Identity is the natural place to surface "what made this node").

### Backend (1 file)

- `src/chronos/cli/web.py` (+50 LOC, including 30-LOC helper + ~10 LOC wire-in)
  - New `_pid_file_path(host, port) -> Path`: `$XDG_RUNTIME_DIR/chronos-web-<host>-<port>.pid` (Linux runtime-state convention) with `/tmp` fallback. Per-`(host,port)` filename so two parallel instances on different ports don't trample each other.
  - New `_reap_stale_pid_file(pid_file, console)`: `os.kill(old_pid, 0)` to probe; `ProcessLookupError` → reap with a `[yellow]note:[/]` message; `PermissionError` (alive but other-user) → leave alone; corrupt file (non-int) → silently nuke. Best-effort throughout (read-only filesystem etc. don't break serving).
  - Wire-in to `web_command`: reap-then-write before banner; cleanup in `finally` next to `store.close()`. Both wrapped in `contextlib.suppress(OSError)` — never block serving on PID-file plumbing.

### Tests (1 file)

- `tests/unit/test_cli_web.py` (+99 LOC, 4 new tests in `TestPidFile`)
  - `test_pid_file_path_uses_xdg_runtime_dir`: `$XDG_RUNTIME_DIR` precedence
  - `test_pid_file_path_falls_back_to_tmp`: env-unset fallback to `/tmp`
  - `test_pid_file_written_and_cleaned_up`: roundtrip — file exists during run with our PID, cleaned up after `web_command` returns
  - `test_stale_pid_file_is_reaped`: pre-seed PID file with PID 999999, verify it's reaped and serving still proceeds

### Skill update (1 file)

- `chronos-web-cron-port-leak` SKILL.md
  - "Long-term fix" section rewritten to "Long-term fix — status" — option 1 (PID file) marked SHIPPED in R114 with test reference. Option 2 (`chronos web --stop`) remains backlog.
  - Documents what the PID file fixes (silent-zombie self-healing) and what it doesn't (TIME_WAIT, alive-process collisions).

## Test results

```
.venv/bin/pytest tests/ -x --no-cov
697 passed, 9 skipped in 28.08s
```

- 697 = 693 baseline + 4 new TestPidFile tests
- 9 skipped = `tests/live/*` (CHRONOS_LIVE != 1 — opt-in only)
- 0 failed
- Adapter zero-regression streak: **R52→R114 = 63 rounds**

Frontend `npx tsc --noEmit`: clean (0 errors).
Frontend `npm run build`: clean (5391 modules, 1.46 MB → 477 KB gzip, 10.5s).

## R122 acceptance scorecard (delta vs R113)

- [x] CLI quickstart ✅ R109 / doctor ✅ R110 / eval ⏳ R115
- [x] CLI help/error UX ✅ R108
- [x] **Cost visibility (ADR-029)** ✅ R111 (verified GREEN)
- [ ] **Evaluation (ADR-030)** ⏳ R115 (next mandatory ADR round)
- [ ] Frontend P0: 5 core pages clean — **3 of 5 R113 P0 closed (F7/F9/F10)**, F8/F11 deferred. (R115 prologue or R114-cont if budget allows.)
- [ ] Onboarding Tour: component exists since R36-D, wiring/polish — **deferred to R115 prologue** (out of budget this round)
- [ ] README中英 + demo GIF — R116
- [ ] 文档站 — R117
- [ ] Demo ≥3 — R118
- [x] Test baseline green — 697 passed
- [x] Adapter zero-regression — 63-round streak intact

距 R122: **R115/R116/R117/R118/R119/R120/R121/R122 = 8 轮**. Phase 6 frontend P0 sweep at 3/5 closed; F8/F11 carried forward.

## Port-leak status

- Pre-flight: `ps -ef | grep chronos web` showed pid 709877 from May 31 17:37 (user's local dev instance, NOT a cron leak — no port bound, may be a long-stale fastapi process). Per skill "user may be running their own — check start time alignment with cron round" — left alone, no kill.
- This round did **not** start `chronos web`, so no new PID file produced. The PID-file logic is exercised by the new tests, not by a live serve in this round.
- **Future cron rounds that DO start `chronos web` will benefit**: stale PIDs auto-reaped, clean exit removes the file. The "silent zombie next round" failure mode is now self-healing.

## What's NOT in this round

- F8 (draft-badge tooltip overlay on node header) — needs visual investigation; AntD Tooltip may be rendering immediately due to a stale `mouseEnter` handler, or it's a CSS z-index/stacking issue. Capture a fresh screenshot in R115 prologue.
- F11 (LandingPage timing CTA copy) — 5-min tweak, not a P0 blocker, batch with R116 README work.
- Onboarding Tour live walkthrough — 6-surface dogfood requires budget that the SIX-compaction recovery slot can't sustain. R115 prologue or R119 E2E will catch any remaining gaps.
- Cron prompt header force-load skill — out of repo scope (lives in cron config). The skill itself has the trigger condition correctly listed (verified on read).

## Next round (R115)

ADR-030 Evaluation. Mandatory ADR read-first. Plan:

1. Read `docs/decisions/ADR-030-evaluation-scoring.md` full text first
2. Optional 30-min prologue: F8/F11 cleanup if R114 left them, plus Onboarding Tour wiring verification
3. Execute ADR-030 scope only (evaluations table, `chronos eval run/list`, `compare --eval`, frontend Score column, 2 built-in evaluators)
4. Acceptance per ADR-030 spec — no scope creep

## Identity check

✅ Still in R107-R122 polish + ADR-029/030 track. R114 closed 3 of 5 catalogued P0 (60%) plus shipped a backlog backend item the cron slots have been chasing for 3 rounds (R109/R112/R113 all leaked). Skill updated to reflect new reality. No new direction, no new ADR, no public-repo flip. R122 still on track at 8 rounds out.
