# R83 — ADR-026 §6 acceptance-gate audit + v0.7.0a2 alpha cut

**Date**: 2026-05-18 (Beijing, ~00:30-01:00 CST)
**Scope**: Documentation-only round + release cut. Audit ADR-026 §6 acceptance criteria, write Arc B slice-3 closing retro, bundle R74→R82 (alpha-stalled since R74) into v0.7.0a2 PEP 440 alpha tag.
**Result**: ✅ 631 passed / 7 skipped (live) / 0 xfail / 0 failed in 17.30s. v0.7.0a2 cut. Adapter-1-3 zero-regression streak R52→R83 = **31 rounds** (new project-history high).

## TL;DR

R83 is the long-overdue release-cut for the Anthropic Agents SDK adapter (Arc B slice 1+2+3). Nine prior rounds (R74-R82) had been accumulating in `[Unreleased]` because R74 explicitly chose "no tag — accumulates" pending slice-3 completion. Slice 3 finished at R82; R83 closes the books: ADR-026 §6 audit (3 of 5 ACs fully closed, 2 partially closed with explicit GA-gating), slice-3 closing retro added in-place to ADR-026, and the PEP 440 alpha bump from `0.7.0a1` → `0.7.0a2`.

## Goal (per CONTEXT.md §6 R83 plan)

Per CONTEXT.md §6 next-round TODO at line ~750+: cut v0.7.0a2 (or a3 — TBD by inspection), audit ADR-026 §6 ACs, close slice 3 retro. Optional: extract `_StubBlock`/`_StubMessage`/`_aiter` fixtures (Option B) if time permits.

**Adjustment from plan**: CONTEXT.md described "v0.7.0a2 already cut at R74" but `git tag --list` shows only `v0.7.0a1`. R74's progress doc explicitly said "no tag — accumulates in [Unreleased]". So R83 cuts **v0.7.0a2** (not a3) bundling R74 + R75-R82 + R83. Logged this discrepancy explicitly so future rounds don't inherit the stale assumption (CONTEXT.md §5 + §6 refresh below corrects it).

**Option B deferred to R84**: fixture extraction adds non-trivial scope (touch 3 unit-test files + create a new `tests/fixtures/` module). With release-cut overhead (8-step skill), staying disciplined and shipping A-only is the right call. R84 entry placed in CHANGELOG `[Unreleased]`.

## Setup

- Pre-flight: `git fetch origin main && git status` clean. HEAD on main, sync'd with origin.
- Pre-flight test: `uv run pytest -q --no-cov` → 631 / 7 / 0 / 0 in 17.24s. Baseline confirmed before edits.

## Observations

### CONTEXT.md §6 inheritance bug (resolved)

Three CONTEXT.md inheritance signals contradicted each other:

1. CONTEXT.md §5 said "latest tag = v0.7.0a1 (R73)".
2. CONTEXT.md §6 R83 plan referenced "v0.7.0a2 cut at R74".
3. `git tag --list` confirmed only `v0.7.0a1` exists.

Source of truth is git. R74 progress doc grep confirmed: line 5 says "no tag — accumulates in [Unreleased]". The R83 plan in §6 had a chained mis-statement that R83 corrects. Lesson logged (no skill update — single-occurrence drift, not pattern-grade).

### ADR-026 §6 honest audit

Pre-R83 state: all 5 ACs were `[ ]` unchecked. R83 audit walk-through:

- **AC-1** (Recorder/Adapter conformance): ✅ closed — R71+R74+R76/R77+R80+R82 all extend the conformance suite at `tests/unit/test_adapter_anthropic_agents.py` and 3 sibling files; 631/7/0/0 baseline holds.
- **AC-2** (live-smoke ≥1 MCP tool): partial. R73 ran a real-OneAPI single-turn smoke (no MCP tool — 9-block sample prompt). MCP tool requires Node.js subprocess + an MCP server fixture — non-trivial to set up in cron env. Defer to v0.7.0 GA gate.
- **AC-3** (override-fork live-smoke): partial. Fork-without-overrides has a real live-smoke (R74 calls `claude_agent_sdk.fork_session` for real). Override-fork (R80 tool-input, R82 tool-result) is fake-SDK only at unit + dogfood level. Defer real-relay version to v0.7.0 GA.
- **AC-4** (dogfood): ✅ closed — 4 dogfoods (`arc_b_slice_1_smoke.py`, `arc_b_slice_2_fork.py`, `dogfood_fork_tool_override.py`, `dogfood_fork_tool_result_override.py`), all exit 0.
- **AC-5** (zero-regression): ✅ closed — `git log` shows no commits to LangGraph/AutoGen/CrewAI adapter source since R51; 31-round streak.

Verdict: alpha-grade green light (AC-1/4/5 fully + AC-2/3 partial = sufficient for `0.7.0a2`). Full GA requires AC-2/3 conversion to fully closed.

### Slice-3 retro narrative

Three sub-slices, all using strict-xfail forcing function pattern:

- **3a** (R76 single-block JOIN, R77 multi-block keyset): writer-side state_after stamps for tool-call linkage.
- **3b** (R79 strict-xfail → R80 implementation): `tool_input_overrides` rewrites tool-call inputs at fork point.
- **3c** (R81 strict-xfail → R82 implementation): `tool_result_overrides` rewrites tool-call results at fork point.

Architecture observation captured in retro: the implementation deliberately keeps two *parallel* recorder fields (`_fork_overrides` + `_fork_result_overrides`) rather than a unified discriminated-union helper. This is intentional design — keeps `_translate(msg)` switch explicit per block kind. R83 audit confirms no accidental coupling.

## Decisions

1. **Cut v0.7.0a2, not a3**. R74 never tagged; CONTEXT.md §6 was wrong. Bundle is "Round 74 + Round 75 + ... + Round 82 + Round 83" in CHANGELOG header.
2. **Use partial-tick `[~]` notation in ADR-026 §6** for AC-2 / AC-3. Plain `[x]` would over-claim; plain `[ ]` would under-credit completed work. The partial-tick + closing note is more honest and creates a clean GA gate.
3. **R57 in-place ADR promotion invariant honored**: do NOT flip ADR-026 Status header. It already reads "Accepted (R69, 2026-05-13)" — that scope-flip happened R69. R83 only ticks the §6 release-time checkboxes, no Status mutation.
4. **Defer fixture extraction (Option B) to R84**. Single-slot SOP discipline; release cut + retro is a full round of work.

## Outputs

### Code / config

- `pyproject.toml`: `version = "0.7.0a1"` → `"0.7.0a2"`
- `src/chronos/__init__.py`: `__version__ = "0.7.0a1"` → `"0.7.0a2"`
- `src/chronos/cli/__init__.py`: status-line v-string + narrative ("Arc B slice 1+2+3 alpha (R70-R83, ..., record + fork + override)" + "streak R52->R83 = 31 rounds")
- `uv.lock`: 1-line version-only delta (`0.7.0a1 → 0.7.0a2` for the chronos-agent project entry; not a hash drift, legitimate to commit)

### Documentation

- `CHANGELOG.md`: rolled `[Unreleased]` → `[0.7.0a2] — 2026-05-18 (Round 74 + ... + Round 83)`; new empty `[Unreleased]` placeholder above it referencing R84 fixture-extraction candidate. Added R83 entries (audit + retro) and "Highlights of v0.7.0a2 / Install / Known caveats / Quality bar" sections to the alpha header block.
- `docs/decisions/ADR-026-arc-b-scope.md` §6: 5 checkboxes ticked (`[x]` ×3, `[~]` ×2 with closing notes); added "Alpha-gate verdict (R83)" paragraph; expanded "In-place promotion marker" with R83 wording; new "Slice-3 closing retro (R83)" sub-section with 3 sub-slices + 3 invariants.
- `docs/CONTEXT.md`: §5 / §6 refreshed (this round's deltas).
- `docs/progress/2026-05-18-round-83.md` (this file).

### Git artifacts (pending steps 6-8)

- Commit + annotated tag `v0.7.0a2` with multi-line release-notes body.
- Push main + tag via gh-proxy mirror.
- GitHub Release page POST via REST API (prerelease=true, make_latest=false to keep v0.6.0 as Latest badge).

## Test evidence

```
$ cd /workspace/chronos-agent
$ uv run ruff check src/ tests/ scripts/        # All checks passed!
$ uv run ruff format --check src/ tests/         # 97 files already formatted
$ uv run mypy src/                                # Success: no issues found in 38 source files
$ uv run pytest -q --no-cov
631 passed, 7 skipped in 17.30s
$ uv run chronos --version
chronos 0.7.0a2
$ grep -nE "⏳|pending|target R[0-9]+" docs/roadmap.md
# (empty — no drift)
```

## What's next (R84 candidates)

1. **Option B fixture extraction** (deferred from R83): create `tests/fixtures/anthropic_agents_stubs.py` consolidating `_StubBlock`, `_StubMessage`, `_aiter` patterns from `test_adapter_anthropic_agents.py`, `test_anthropic_agents_fork_tool_override.py`, `test_anthropic_agents_fork_tool_result_override.py`. Pure code-quality, no functional change. Estimated 1 cron slot.
2. **GA gating: AC-2 / AC-3 live-smoke** — set up MCP server fixture + Node.js subprocess in CI; convert partial-tick to full-tick. Larger scope; might need infra work outside chronos-agent repo (Node 20 install, MCP server installable via npm).
3. **Arc B slice 4 scoping** — what's next after override-fork for the Anthropic Agents adapter? CONTEXT.md §6 long-list candidates: cross-framework diff dogfood (LangGraph run vs Anthropic Agents run in same compare view, no new code), or pivot to Arc B slice 2 (Pydantic AI / Letta as second framework on this Arc — would need fresh ADR per [ADR-026 §C2/C3 fallback]).

R84 first-choice: **Option B fixture extraction** (cheap, finishes the slice-3 cleanup, keeps adapter streak alive at 32 rounds).
