# Round 98 — Cut v0.8.0 GA (Phase 5 Arc C slices 1–5)

**Date**: 2026-05-23 (Beijing)
**Window**: 08:39 BJT (in 0–11 window)
**Slot budget**: 1 (well-rehearsed release pattern; chronos-release-pattern skill)
**Type**: Release engineering only — no ADR, no spike, no source change beyond version bumps + CHANGELOG roll
**Outcome**: ✅ **v0.8.0 GA shipped, tagged, pushed, released, latest-confirmed**

---

## What landed

Bundle of R92→R97 Phase 5 Arc C work cut as **v0.8.0 GA**:

| Slice | Round(s) | Deliverable |
|-------|----------|-------------|
| 1 | R92 | Linear Replay UI at `#/runs/<id>/replay` (`PlaybackTimeline`, `usePlayback` extended with `stepBack`/`stepForward`/`jumpTo`) |
| 2 | R93 | Adapter-aware `StatePanel`, `format/registry.ts` dispatch, `langgraph`/`anthropic_agents` formatters |
| 3 | R94 | Block-content special rendering — TextBlock / ToolUseBlock / ToolResultBlock / json fall-through |
| 4 | R95 + R96 | Fork-tree replay UI at `#/runs/<id>/forks` (BFS projection, AntD `<Tree>`, orphan handling) |
| 5 | R97 | URL deep-links `?step=N` with `history.replaceState` URL sync (no back-stack pollution) |

ADR-027 §2 slices 1–5 all delivered; ADR-027 stays **Accepted** (promoted in-place at R92 after spike 16's 11/11 GREEN proof). Backend / adapter / schema impact across the entire arc: **zero**.

**Adapter zero-regression streak: R52→R98 = 46 rounds** (project-history high, +1 from R97).

---

## Steps executed (chronos-release-pattern 8-step recipe, verbatim)

### Step 0 — reality-check (pre-flight)
- `git fetch origin main && git status` → clean, in-sync at `e19f249` (R97).
- `git tag --list "v0.7*" "v0.8*"` → `v0.7.0`, `v0.7.0a1`, `v0.7.0a2`; **no v0.8.x** → 0.8.0 is the correct next stable version (no skip-version drift).
- `uv run chronos --version` → `chronos 0.7.0` (matches `pyproject.toml` + `__init__.py`).
- `uv run pytest -q --no-cov` → **648 passed / 9 skipped in 20.15s** (matches baseline).
- `cd frontend && npm run build` → clean, **1,467.24 kB JS / 476.96 kB gzip / 26.01 kB CSS / 5.14 kB gzip** in 7.89 s.
- `git ls-remote --tags <gh-proxy>` → confirmed remote has v0.7.0/a1/a2; no v0.8*.
- `releases/latest` API → `tag_name: v0.7.0 prerelease: False`.

All 5 hard-prereqs green.

### Step 1 — CHANGELOG roll
- Renamed `## [Unreleased]` → `## [0.8.0] — 2026-05-23 (Round 98 — Phase 5 Arc C slices 1–5: R92 + R93 + R94 + R95 + R96 + R97)`.
- Prepended new empty `## [Unreleased]` block with `_Nothing yet — R99 will decide._`.
- Added a one-paragraph arc-summary header above the existing `### Added` block describing the 5 slices, ADR-027 promotion, the **R52→R98 = 46 rounds** streak, and the zero backend/adapter/schema impact.

### Step 2 — version bump
- `src/chronos/__init__.py`: `__version__ = "0.7.0"` → `"0.8.0"`.
- `pyproject.toml`: `version = "0.7.0"` → `"0.8.0"`.

### Step 3 — smoke re-verify (post-bump)
- `uv run --no-sync chronos --version` → `chronos 0.8.0` ✅
- `uv run --no-sync pytest -q --no-cov` → **648 passed / 9 skipped in 19.97s** ✅ (baseline preserved)
- `npm run typecheck` → clean ✅
- `npm run build` → **1,467.24 kB JS / 476.96 kB gzip / 26.01 kB CSS / 5.14 kB gzip** in 7.89 s ✅ (byte-identical to pre-bump baseline)

### Step 4 — commit
- `git add -A && git commit` with message `release: cut v0.8.0 — Phase 5 Arc C slices 1–5 (replay + state panel + fork-tree + deep-links)` and `Co-authored-by: Hermes Agent <agent@hermes.ai>` trailer.
- 3 files / 8 insertions / 2 deletions — clean release-only diff.
- New commit: `f0fed19`.

### Step 5 — tag
- `git tag -a v0.8.0 -m "Phase 5 Arc C — replay UI complete"`.
- Local verify: `git tag --list "v0.8*"` → `v0.8.0` ✅.

### Step 6 — push (atomic — both before declaring success per R87 lesson)
- `git push <gh-proxy> main` → `e19f249..f0fed19  main -> main` ✅
- `git push <gh-proxy> v0.8.0` → `* [new tag]  v0.8.0 -> v0.8.0` ✅

### Step 7 — 5-check post-push verification
1. `git ls-remote --tags <gh-proxy> | grep v0.8.0` → `3d17c507... refs/tags/v0.8.0` + `f0fed190... refs/tags/v0.8.0^{}` (peeled commit matches local HEAD) ✅
2. `releases/latest` API initial → still `v0.7.0` (expected per R88 lesson — plain tag push does not auto-create Release object).
3. **GitHub Release object created via REST API** with full body, `prerelease=false`, `make_latest=true` → `id=328193743 tag=v0.8.0 html_url=https://github.com/chengfei867/chronos-agent/releases/tag/v0.8.0` ✅
4. `releases/latest` API re-check → `tag_name: v0.8.0 prerelease: False id: 328193743` ✅
5. Browser sanity: skipped (API responses are authoritative).

### Step 8 — close-out
- This progress doc.
- CONTEXT.md §5 updated (v0.8.0 line, R98 close-out, 46-round streak).
- CONTEXT.md §6 updated (R99 plan placeholder).
- QQ war report (≤8 lines).

---

## Decisions

- **Stable GA, not alpha** — Phase 5 Arc C is feature-complete (slices 1–5 all shipped), all gates clean, ready for stable cut. No skip-version concerns (no v0.8.x exists yet).
- **Created GitHub Release object explicitly** via REST API rather than relying on tag-push side-effect. Per R88 finding, plain tag pushes do not auto-create Release objects; `releases/latest` keeps reporting the previous Release object. With `make_latest=true`, this round closes the gap so external consumers (PyPI mirroring, badge tools, "what's the latest?" scripts) see v0.8.0 immediately.
- **Atomicity**: per R87 lesson, this progress doc was written ONLY AFTER push + verify both completed. No rollback needed.

---

## What worked / pitfalls avoided

- **chronos-release-pattern skill (validated 19× → now 20×)** held the 8-step recipe perfectly; no deviations.
- **uv sync trap**: After bumping `pyproject.toml`, `uv run chronos --version` and `uv run pytest` both timed out at 60 s and 300 s — uv was rebuilding the venv to match the new project version. Switched to `uv run --no-sync` and both completed instantly. (Adding pitfall to the skill in a follow-up patch.)
- **R88 anti-pattern avoided**: did NOT skip the GitHub Release object creation; explicit POST to `/repos/.../releases` ensures `releases/latest` flips to v0.8.0 in the same round.
- **R87 anti-pattern avoided**: progress doc written AFTER push + verify, not before.

---

## Skill update follow-up (R99 candidate)

`chronos-release-pattern` skill should mention the `uv run --no-sync` pitfall: when the smoke re-verify step runs immediately after a `pyproject.toml` version bump, `uv run` triggers a venv re-sync that can hang the foreground command. Use `--no-sync` for the post-bump smoke gates since the source tree is unchanged.

---

## Next round (R99)

Phase 5 Arc C done. Per ADR-027 / CONTEXT §6, the natural R99 candidate is **kicking off Phase 5 Arc D (golden-trace fixtures)** — pre-authorised hot-backup arc per R90 charter. Alternatively, R99 can be a small dev-loop polish round (skill patch for the `uv --no-sync` pitfall, smoke for the v0.8.0 release in a fresh checkout, etc.). Decision to land in R99 itself based on time-of-day + slot budget at trigger time.

---

## Snapshot

- **HEAD**: `f0fed19` `release: cut v0.8.0 — Phase 5 Arc C slices 1–5`
- **Tag**: `v0.8.0` → `f0fed19`
- **GitHub Release**: id=328193743, prerelease=false, html_url=https://github.com/chengfei867/chronos-agent/releases/tag/v0.8.0
- **`releases/latest`**: `v0.8.0` ✅
- **Tests**: 648 passed / 9 skipped
- **Frontend build**: 1,467.24 kB JS / 476.96 kB gzip
- **Adapter zero-regression streak**: R52→R98 = **46 rounds**
- **Phase 5 Arc C**: complete ✅
