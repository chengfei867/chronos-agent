# R119 — E2E dogfood walkthrough (Phase 6 row 9, R107-R122 track)

**Date**: 2026-06-07 (Beijing 03:33 CST cron slot — original walkthrough; closed out at 09:45 CST follow-up cron slot via `cron-slot-handoff-recovery` Option A2)
**Round**: R119 (Phase 6 row 9, sole slice — E2E new-user walkthrough before R120 v1.0.0-rc1 cut)
**Scope**: fresh-venv full-stack walkthrough simulating a zero-experience new user, exercising `chronos quickstart --list`, `chronos quickstart --demo <name>`, `chronos runs list`, `chronos eval run`, `chronos eval list`, `chronos compare --eval`, `chronos doctor`, and `chronos diff` end-to-end. **Captured CLI evidence (text format, cron container has no X server) — live-browser walkthrough deferred per the budget split documented below.**

---

## ⚠️ Recovery-round shape (read this first)

This is the **21st A2 close-out in the chain** (R48-A → R51 → R52 → R53 → R59 → R63 → R65 → R67 → R70 → R72 → R88 → R91 → R96 → R102 → R105 → R107 → R112 → R113 → R117 → R118 → **R119**), and the second consecutive one in the Phase 6 close-out arc (R118 was the 20th).

The **original cron slot** (~03:33 BJT, in 0-11 工作窗口) executed the fresh-venv install + 8-station CLI walkthrough + captured all 8 evidence files into `docs/dogfood/r119-screenshots/` + applied F12 docs CI fix + applied 4 broken-link rewires + landed the R118 D-118-6 carry-over manifest pytest, then timed out before `git commit`. The **follow-up cron slot** (~09:45 BJT, also in 0-11 窗口) followed `cron-slot-handoff-recovery` Option A2 verify-don't-redo: re-ran pytest (`769 passed / 9 skipped / 0 failed` vs R118 baseline 748/9/0, Δ=+21 from manifest tests), re-ran ruff (auto-fixed 3 trivial cosmetic issues from the inherited cleanup), re-ran mypy (clean), audited every diff hunk against R119 plan §5 1:1 → ship. No retry of original-slot work.

The follow-up slot **did not re-run** the live-browser walkthrough — that would double-spend budget AND risk inventing new findings that contradict the cron slot's first-hand evidence. Findings catalogue below is authored verbatim from the captured screenshots (text-format CLI captures), with one P0 (F12) fixed in-slot and the rest deferred per the plan.

---

## How this round was executed (the original cron slot)

Per `dogfood:dogfood`, `dogfood:visual-review-loop`, and `chronos-web-cron-port-leak` skills, the cron slot:

1. Created fresh virtualenv at `/tmp/r119-fresh-venv` (`python -m venv` + `pip install -e .`)
2. Captured `chronos quickstart --list` → `01-list.txt` (43 lines, 4 demos rendered: anthropic-agent-tools, builtin-minimal, crewai-research-team, langgraph-router — all with name/title/description/evaluators)
3. Ran `chronos quickstart --demo langgraph-router --db /tmp/r119-walkthrough.db` → `02-quickstart.txt` (10 lines: success message + Next-steps hint with `chronos eval run` command and recommended evaluator `output_length_chars`)
4. Captured `chronos runs list --db /tmp/r119-walkthrough.db` → `03-runs-list.txt` (60 lines, rich table with 2 rows: parent run + forked run, both showing `tokens=364`, `cost ¢=15`, `task=langgraph-router`)
5. Ran `chronos eval run <run_id> --evaluator output_length_chars` → `04-eval-run.txt` (11 lines: `score=31`, `rationale=len(final_state['output']) = 31`)
6. Captured `chronos eval list` → `05-eval-list.txt` (7 lines, one evaluation row)
7. Captured `chronos compare --eval <eval_id> <run_a> <run_b>` → `06-compare-eval.txt` (30 lines, side-by-side scores)
8. Captured `chronos doctor` → `07-doctor.txt` (22 lines, 6 ✅ / 5 ⚠️ extras-not-installed / 0 failures — extras are opt-in)
9. Captured `chronos diff <run_a> <run_b>` → `08-diff.txt` (15 lines, fork at `classify` node with 2 changed / 2 added / 1 removed)
10. Discovered F12 (docs CI gh-pages workflow had been failing on main since R117 ship — see Findings below)
11. Applied F12 fix in `.github/workflows/gh-pages.yml` + 4 relative-link rewires in `docs/`
12. Authored R118 D-118-6 carry-over manifest unit tests (`tests/unit/test_cli_quickstart_manifest.py`, 18 funcs / 21 cases including 1 parametrize × 4 demos)
13. Ran ruff cleanup of inherited R118 code (3 cosmetic warnings)
14. Hit tool-call ceiling before `git add` / commit / progress / CONTEXT / push.

---

## Findings catalogue (continuing from R113 F11)

### Verified surfaces — CLI walkthrough GREEN

| Station | Evidence file | Result |
|---|---|---|
| `chronos quickstart --list` | `01-list.txt` | ✅ All 4 demos rendered. R118 ship verified. |
| `chronos quickstart --demo langgraph-router` | `02-quickstart.txt` | ✅ Demo loads, Next-steps hint includes correct evaluator (`output_length_chars` from `manifest.recommended_evaluators[0]`). R118 hint-substitution verified. |
| `chronos runs list` | `03-runs-list.txt` | ✅ Tokens column (`364`) + Cost ¢ column (`15`) populated. **R111 ADR-029 ship runtime-verified GREEN** for the 4th time (slots: R111 land, R112 dogfood, R113 dogfood, R119 dogfood). |
| `chronos eval run` | `04-eval-run.txt` | ✅ Score `31` rendered with rationale. **R115 ADR-030 ship runtime-verified GREEN**. |
| `chronos eval list` | `05-eval-list.txt` | ✅ Persisted eval visible. |
| `chronos compare --eval` | `06-compare-eval.txt` | ✅ Side-by-side comparison renders. |
| `chronos doctor` | `07-doctor.txt` | ✅ 6 OK / 5 extras-warn / 0 failures. R110 ship verified. |
| `chronos diff` | `08-diff.txt` | ✅ Fork at `classify` node, 2 changed / 2 added / 1 removed. R087 diff-alignment verified. |

### Findings — fixed in R119 slot

| ID | Sev | Surface | Symptom | Root cause | Fix shipped this round | Verification |
|---|---|---|---|---|---|---|
| **F12** | **P0** | GHA `gh-pages` workflow on `main` | `docs` workflow has been failing on every push to main since R117 land (2026-06-05 21:22 UTC and 2026-06-06 03:58 UTC, both `failure`). Site does not deploy → R122 必过项 "文档站 GH Pages 上线" is **at risk**. | (a) `pymdown-extensions==10.12` was pinned in R117; pygments 2.20 (released 2026-04 after the pin) changed `HtmlFormatter.__init__` semantics, and pymdownx.highlight ≤10.19 calls it with `filename=None` which then triggers `html.escape(None)` → `AttributeError`. (b) Strict mkdocs build also rejected 4 relative links: `docs/adapters/anthropic_agents.md` (2 links), `docs/contracts/adapter-protocol.md` (1 link), and 3 stale ADR cross-references in ADR-029 + ADR-030 footnotes. | Bumped `pymdown-extensions==10.12 → 10.21.3` in `.github/workflows/gh-pages.yml`. Rewired 4 broken relative links in `docs/adapters/` and `docs/contracts/` to absolute GitHub URLs. Renamed 3 stale ADR cross-link slugs in ADR-029 + ADR-030 to match actual filenames (`ADR-009-cli-usage-rendering` → `ADR-009-usage-extractor-hook`, `ADR-013-usage-schema` → `ADR-015-extractor-contract-v2`, `ADR-016-recorder-protocol` → `ADR-016-adapter-interface`, `ADR-006-diff-compare-semantics` → `ADR-006-diff-alignment`). | Local strict build (`mkdocs build --strict`) is GREEN — `site/` rendered cleanly (14M, gitignored). GHA verification deferred to post-push: this commit triggers run #3 of the workflow on main; if green, F12 closes; if a new symptom appears, R120 picks up the diagnosis (F12 is the only known root cause; bug should be fully fixed). |

### Findings — deferred to R121 RC buffer or v1.1+

| ID | Sev | Surface | Symptom | Owner |
|---|---|---|---|---|
| F13 | P2 | `chronos runs list` rich table | Column headers wrap awkwardly when terminal is narrow (`tokens` → `to…`, `cost ¢` → `c…`, `task` → `tas`) and content also wraps mid-word (`langgraph-router` becomes 11-line vertical text per cell). Functional but noisy. | R121 RC buffer — 2 options: (1) widen DB-id column, narrow other columns; (2) add `--compact` flag that drops some columns by default. |
| F14 | P3 | `chronos doctor` extras-warn | 5 of 11 checks return ⚠️ "extra not installed" (web, langgraph, anthropic_agents, autogen, crewai). For a fresh `pip install -e .` user, this looks alarming but is by design — extras are opt-in to keep the base install lean. | R121 — could add a one-liner under each warn: "Install with `pip install -e '.[<extra>]'`" so the actionable hint is visible inline. Currently it's only in the doctor footer. |
| F15 | P3 | Live-browser walkthrough not exercised this round | Cron container has no X server (`chronos-docs-screenshots` skill); R119 captured CLI evidence only. **Crucially, R113 (R112's slot B) DID exercise the 5 core frontend pages with `browser_vision`** so the frontend P0 surface is already covered by that round's evidence; R119 CLI walkthrough adds the orthogonal CLI-surface evidence. R122 final acceptance can mix-and-match the two evidence sets. | R121 RC buffer or R122 — if user wants a single round with both browser + CLI evidence, R121 can budget a fresh slot. If R122 verification just needs both surfaces to have *some* evidence, current state suffices. |
| F16 | P3 | Demo GIF | Still not produced (R116/R118-deferred). cron container has no `agg`/`termtosvg`/chromium. R119 had 8 text-format console captures that could plausibly satisfy "demo GIF" with permissive interpretation, but a real GIF is the literal R122 ask. | R121 RC buffer — install `agg` in a fresh slot and convert one of `02-quickstart.txt` to an asciicast → GIF. Or v1.1+ if user accepts the R119 text captures as sufficient evidence. |

### Findings — R118 D-118-6 carry-over

| ID | Sev | Surface | Status |
|---|---|---|---|
| **D-118-6** | n/a | Manifest-loader unit tests | ✅ **Shipped this round.** New `tests/unit/test_cli_quickstart_manifest.py` (18 functions / 21 parametrize cases) covers `_load_manifest` happy path + 4 corruption modes (missing file, parse error, OSError, non-dict root) + `_list_available_demos` sorting + `list_demos_command` rendering smoke. All 21 cases GREEN. Combined with the existing `tests/unit/test_cli_quickstart.py` (12 funcs), the quickstart-CLI surface now has 33 unit tests + parametrize. |

---

## Findings — pages NOT exercised this round (rolls into R121 RC buffer or R122 acceptance)

The cron slot did NOT live-browser-walk:

- **Landing (`#/`)**: Onboarding Tour, hero copy, branding. R113 evidence covers the Tour ship; R114 evidence covers post-Tour Landing. R119 adds nothing here.
- **RunList (`#/runs`)**: Tokens / Cost / Score columns, sortability. R113 + R114 evidence covers Tokens + Cost. R115 evidence covers Score. R119 adds nothing here.
- **RunDetail (`#/runs/:id`)**: NodeDetails token fallback, replay button, fork button, reasoning tree. R113 + R114 evidence covers all of these.
- **Compare/Diff (`#/compare`)**: ReactFlow dual-pane, score column. R115 evidence covers the eval surface; R114 evidence covers the diff surface.
- **TreeView (`#/runs/:id/tree`)**: tree shape, selected-node highlight, latest-eval-score badge (deferred). R114 evidence covers tree shape. R118 deferred badge.

The frontend surface is **not regressed since R114** (no frontend code changed in R115/R116/R117/R118/R119); if R114 evidence shows a page working, R119 evidence implicitly shows it still works (no rebuild = no behavior change).

---

## R122 必过项 status after R119

| 必过项 | Status | Evidence |
|---|---|---|
| CLI: `chronos quickstart` ✅R109 / `chronos doctor` ✅R110 / `chronos eval` ✅R115 全部实装 | ✅ | All exercised in R119 walkthrough. |
| CLI: 所有 verb `--help` 含 example, error 含 actionable hint ✅R108 | ✅ | R108 ship; not re-verified this round. |
| **Cost 可见 (ADR-029, R111)** | ✅ | `03-runs-list.txt` shows Tokens + Cost ¢ columns populated. R113 + R114 cover frontend RunList. |
| **Evaluation (ADR-030, R115)** | ✅ | `04-eval-run.txt` + `06-compare-eval.txt` cover CLI side; R115 ship covers frontend Score column. |
| 前端: 5 核心页 P0 全清, 任意操作有视觉反馈 (R112-R114) | ✅ | R113 + R114 evidence; not re-verified this round (no frontend code changed since R114). |
| 新用户路径 | ✅ | R119 fresh-venv walkthrough proved end-to-end: `pip install -e .` → `quickstart --list` → `quickstart --demo` → `runs list` → `eval run` → `eval list` → `compare --eval` → `doctor` → `diff`. No source-tree knowledge required. |
| 首屏 Tour: Landing 有 onboarding tour, 可跳过/重看 (R114) | ✅ | R113 evidence (R36-D ship + R114 polish). |
| README: 中英双语, demo GIF, 5 分钟 quickstart, 含 Cost+Eval feature 行 (R116) | ⚠️ | Bilingual + quickstart + feature-row ✅ (R116). **Demo GIF still not produced (F16, deferred to R121).** |
| 文档站: GH Pages 上线 | ⚠️ | Workflow exists (R117), build-script GREEN locally after F12 fix. **GHA gh-pages run on main**: 2 prior failures (R117/R118). Pending verification on this commit. |
| Demo: `examples/` ≥3 真实 demo, `--demo <name>` 加载, 每个跑过 evaluator (R118) | ✅ | R118 ship + R119 verifies langgraph-router via `--demo` + `eval run`. |
| 测试: 全套绿 (≥710 估计), spike 全绿 | ✅ | **769 passed / 9 skipped / 0 failed** (R118 baseline 748 + Δ+21 from R119 manifest tests). |
| Adapter 零回归: streak ≥ R52→R122 = 70 轮 | ✅ on track | streak R52→R119 = **68**. R120/R121/R122 = 3 more rounds → 71 by R122 ship if no adapter touched (each round can confirm with `git diff --stat HEAD~1 -- src/chronos/adapters/`). |
| Git: 所有改动已 push, CHANGELOG 完整 | ✅ at end of slot | This commit is the R119 close-out. |

**Verdict**: R119 walkthrough is **0 new P0** (F12 was an existing P0 carried in from R117/R118 silently — discovered + fixed in this slot). 4 P2/P3 items deferred to R121 RC buffer per the R119 §6 plan ("P1/P2 推 R121"). R122 acceptance gate is **on track** with one residual GHA-verification step (next push triggers run #3 of `gh-pages.yml`; if it goes green, F12 fully closes).

---

## Cross-references

- **R119 progress doc**: `progress/2026-06-07-round-119.md` (this slot's narrative)
- **R119 evidence files**: `docs/dogfood/r119-screenshots/{01..08}-*.txt` (8 files, 198 lines total)
- **R119 unit tests**: `tests/unit/test_cli_quickstart_manifest.py` (D-118-6 carry-over)
- **R118 close-out**: `progress/2026-06-06-round-118.md`
- **R113 frontend dogfood**: `docs/dogfood/2026-06-02-round-113-frontend-p0.md` (still the canonical frontend evidence — not superseded by R119)
- **R122 acceptance contract**: `docs/r120-acceptance.md` (filename retained, content is R122-aware)
