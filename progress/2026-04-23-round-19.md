# Round 19 — Cut v0.1.4 release (R17 + R18 bundle)

**Date**: 2026-04-23
**Duration**: ~25 min (clean release, R13/R16 pattern已成熟)
**Outcome**: v0.1.4 tagged + pushed. R17 supervisor dogfood + R18 swarm dogfood 的 bug fix 正式打包成 release。零 code 改动（纯 release packaging）。

## Goal

R18 把 ADR-012 multi-LLM-per-node token 修复做完后，R17 + R18 两轮的 bug fix 都还没 cut release（CHANGELOG [Unreleased] 空，`chronos info` 仍报 v0.1.3）。R19 的唯一任务 = 按 R13 / R16 已成熟的 release pattern 把 R17+R18 bundle 打包成 v0.1.4。

## What happened

纯 release cut，无争议、无新 decision。严格按 pattern 走：

1. grep `v0.1.3` / `0.1.3` 残留 → 只在 3 个 live 文件命中（`src/chronos/__init__.py`, `pyproject.toml`, `src/chronos/cli/__init__.py::info`），其余都是 CONTEXT.md 历史描述 + progress docs（正确保留）
2. Bump `__version__ = "0.1.3"` → `"0.1.4"`
3. Bump `pyproject.toml::project.version` 同步
4. CLI 状态行追加 `"+ multi-LLM-per-node accumulation, v0.1.4"`
5. CHANGELOG `[Unreleased]` → `[0.1.4] — 2026-04-23 (Round 17 + Round 18)`，theme "Real-world dogfood finds silent token undercount"，分 Fixed (R18) / Fixed (R17) / Numbers / Notes 四个子段
6. 验证：`ruff check` 绿 / `ruff format --check` 绿 / `pytest` 247/247 绿 / `chronos info` 报 `v0.1.4` ✓
7. `git add -A` → `git checkout -- uv.lock`（二次 reset 后干净）→ commit
8. `git tag -a v0.1.4`，push main + push tag → 都走 gh-proxy.com 成功

`git log --oneline -2`:
```
a2df15c R19: cut v0.1.4 release — R17+R18 bundle (dogfood-driven token fidelity fixes)
dd770b7 R18: fix silent multi-LLM-per-node token undercount (ADR-012)
```

## Numbers

- Files touched: 4 (`CHANGELOG.md`, `pyproject.toml`, `src/chronos/__init__.py`, `src/chronos/cli/__init__.py`)
- Code lines changed: +37 / -4
- Tests: 247/247（未变，纯 release）
- Coverage: 93%（未变）
- Tag: **v0.1.4** pushed 到 `origin` via gh-proxy.com
- 耗时: ~25 min（R16 cut v0.1.3 用了 ~30 min，这次略快，pattern 熟了）

## Release pattern 再次验证

R13 (v0.1.2) → R16 (v0.1.3) → R19 (v0.1.4)，同一套 7 步流程跑了 3 次无任何意外：

> `grep prev version` → `bump __version__` + `bump pyproject` + `CLI 状态行` → `CHANGELOG [Unreleased]→[x.y.z]` → `ruff+format+pytest+chronos info 验证` → `commit` → `tag -a + push + push tag`

这个 pattern 进入肌肉记忆了。

## What didn't come up

- 无 test 改动（pure release）
- 无新 ADR（纯 packaging）
- 无 schema / API 改动
- 无 dogfood / no external issue

## Next

R20 选项（接 R18 末尾的 R19-B/C/D 继续延伸）:

- **R20-A (推荐)**: 第三个 dogfood target — `langgraph-bigtool` (tool-selection pattern) 或 Tavily-research-agent 组合。连续 3 轮 dogfood 才够支撑 ADR-008 "lift boundary" 决策；2 轮仍是弱证据
- **R20-B**: 把 R17 Finding #2 (checkpointer silent requirement) 和 R18 Finding #3 (`state_before` 缺失) 写进 `docs/getting-started.md` + 补 Recipes pattern。0.5 轮，适合白天短会话
- **R20-C**: Package & publish v0.1.4 到 PyPI — 跳出 self-dogfood 请外部用户进来。风险: 外部用户可能要求破坏性改动; 收益: 真反馈
- **R20-D**: ADR-008 formal follow-up — 把"2轮dogfood零需求"写成短 ADR，正式 mark boundary 为 evidence-backed。0.3 轮

**强推 A**（dogfood trajectory 还有 cheap evidence 可拿，不要过早转 C）。B/D 可以做白天的短会话填充。

## 硬约束（延续）

与 R18 一致，无变化。下一个 ADR 编号: **ADR-013**。
