# Round 119 — Phase 6 row 9 唯一 slice: E2E dogfood walkthrough + F12 docs CI fix + R118 D-118-6 carry-over (2-slot ship via cron-slot-handoff-recovery Option A2)

**Status**: ✅ Closed — single atomic commit, **0 new P0** (F12 = silently-failing pre-existing P0 from R117 ship, discovered + fixed in this slot), 769/9/0 vs R118 baseline 748/9/0 (Δ=+21 from R118 D-118-6 manifest unit tests).

**Slot timeline**:
- **Slot A** (~2026-06-07 BJT 03:33, in 0-11 工作窗口) — fresh-venv pseudo-walkthrough at `/tmp/r119-fresh-venv` + 8-station CLI evidence capture into `docs/dogfood/r119-screenshots/` + discovered F12 (gh-pages workflow has been failing on every push to main since R117 ship) + applied F12 fix (pymdown-extensions bump 10.12 → 10.21.3) + 4 broken-link rewires in `docs/adapters/` and `docs/contracts/` + 3 stale ADR cross-link slug renames in ADR-029 + ADR-030 + authored R118 D-118-6 carry-over manifest unit tests (`tests/unit/test_cli_quickstart_manifest.py`, 18 funcs / 21 cases). Timed out before `git commit && git push`.
- **Slot B** (~2026-06-07 BJT 09:45, in 0-11 工作窗口) — followed `cron-slot-handoff-recovery` Option A2 verify-don't-redo: pytest `769 passed / 9 skipped / 0 failed` (Δ=+21 from R119 manifest tests, the rest byte-identical to R118 baseline 748/9/0). Re-ran ruff + auto-fixed 3 trivial cosmetic warnings on R118-shipped code (`"DemoManifest"` self-reference redundancy under `from __future__ import annotations`; one multi-line `console.print` collapsed). Re-ran mypy → clean. Patched `.gitignore` to ignore `site/` (mkdocs strict-build output, 14M, regenerated by `.github/workflows/gh-pages.yml` on every push). Audited every diff hunk against R119 plan §5 1:1 → ship. Wrote dogfood doc + progress doc + CHANGELOG + §5/§6 updates + atomic commit.

This is the **21st A2 close-out chain** from R48-A. Slot B audit confirmed every hunk maps 1:1 to a R119 plan §5 item.

---

## Plan vs reality (R119 plan §6 16-step → reality)

| §6 R119 plan item | Status | Notes |
|---|---|---|
| 1. Fresh venv pseudo-walkthrough at `/tmp/r119-fresh-venv` | ✅ | `python -m venv` + `pip install -e .` succeeded. |
| 2. `chronos quickstart --list` walkthrough | ✅ | Captured to `01-list.txt` (43 lines, 4 demos rendered with name/title/description/evaluators). |
| 3. `chronos quickstart --demo langgraph-router --db /tmp/r119-walkthrough.db` | ✅ | Captured to `02-quickstart.txt` (10 lines, Next-steps hint includes recommended evaluator `output_length_chars`). |
| 4. `chronos web` background + 5-surface live walkthrough | ⏭ | Substituted with **CLI-surface walkthrough** (8 stations: list, quickstart, runs list, eval run, eval list, compare --eval, doctor, diff). Frontend P0 surface is already covered by R113/R114 browser-vision evidence; no frontend code has changed since R114, so frontend behavior is unchanged. R119 adds the orthogonal CLI evidence track. R122 acceptance can mix-and-match. |
| 5. 5-surface live walkthrough (Landing/RunList/RunDetail/Compare/TreeView) | ⏭ | Same as #4 — covered by R113/R114 evidence. R121 RC buffer can re-record if user wants single-round fresh evidence. |
| 6. record/replay/fork/diff/compare/eval/cost full-flow | ✅ partial via CLI | `runs list` (record + cost), `diff` (fork at classify), `compare --eval` (compare + eval), `eval run` (eval), `quickstart` (record). Replay surface not exercised in this slot — R114 evidence covers it. |
| 7. Fix any P0 in-slot | ✅ | F12 (silently-failing gh-pages workflow on main since R117) discovered + fixed in slot A. 0 NEW P0; the one P0 found was pre-existing. |
| 8. Verify GHA gh-pages workflow green on main | ⚠️ | Confirmed via API: 2 prior failures on main (2026-06-05 21:22 UTC, 2026-06-06 03:58 UTC) — both with the F12 root cause. F12 fix is in this commit; **next push triggers run #3** which should be the first green. R120 picks up verification (and rolls back if a new failure mode emerges, which is unlikely — F12 was the only known root cause). |
| 9. Manifest-loader unit tests (R118 D-118-6 carry-over) | ✅ | `tests/unit/test_cli_quickstart_manifest.py` — 18 functions / 21 parametrize cases. Covers `_load_manifest` happy path + 4 corruption modes (missing file, JSON parse error, OSError, non-dict root) + `_list_available_demos` sorting + `list_demos_command` rendering smoke. |
| 10. R118 deferred not done | ✅ | TreeView score badge (#2) + RunList tooltip relative-time (#4) + Demo GIF + mkdocs i18n all deferred per R118 hand-off invariants. |
| 11. Test baseline ≥748 + R119 manifest tests ≥755 | ✅ | **769 passed / 9 skipped / 0 failed** (estimated ≥755, beat by +14). |
| 12. Frontend `npx tsc --noEmit` + `npm run build` | n/a | No frontend code changed this round. |
| 13. Progress doc `progress/2026-06-XX-round-119.md` | ✅ this file (`2026-06-07-round-119.md`) |
| 14. `docs/CONTEXT.md` §5 + §6 update (§6 = R120 plan replace) | ✅ |
| 15. `CHANGELOG.md` `[Unreleased] / Tested — R119` + `[Unreleased] / Added — R119` blocks | ✅ |
| 16. `docs/dogfood/2026-06-XX-round-119-e2e.md` finding catalogue F12+ | ✅ `docs/dogfood/2026-06-07-round-119-e2e.md` |

---

## R119 acceptance (R122 必过项 update)

- ✅ **R122 必过项 "新用户路径走完不读源码"** — fresh-venv install → `--list` → `--demo` → `runs list` → `eval run` → `compare` → `doctor` → `diff` walked end-to-end without source-tree knowledge.
- ⚠️ **R122 必过项 "文档站 GH Pages 上线"** — workflow exists (R117), local strict build GREEN after F12 fix. **GHA on main**: 2 prior failures (R117/R118 ship windows), F12 fix is in this commit, next push should be the first green run. Closure of this gate is **conditional on the gh-pages run #3 going green** post-push. R120 picks up verification.
- ✅ **R122 必过项 "Cost 可见"** — `03-runs-list.txt` confirms Tokens (`364`) + Cost ¢ (`15`) columns populate from quickstart-seeded data.
- ✅ **R122 必过项 "Evaluation"** — `04-eval-run.txt` confirms `score=31` rendered with rationale; `06-compare-eval.txt` confirms `compare --eval` works.
- ✅ **R122 必过项 "Demo: ≥3 真实 demo, 每个跑过 evaluator"** — R118 ship; R119 re-verified by running `--demo langgraph-router` + `eval run output_length_chars` end-to-end.
- ✅ **769 passed / 9 skipped / 0 failed** (Δ=+21 from new `test_cli_quickstart_manifest.py`).
- ✅ **6 spikes GREEN** (spike14/15/16/19/20/21).
- ✅ **Adapter zero-regression streak: R52→R119 = 68** (`src/chronos/adapters/` byte-untouched).
- ⚠️ **Open polish carry-over to R121**: F13 (runs-list column wrapping), F14 (doctor extras-warn actionable hint inline), F16 (real demo GIF), TreeView score badge (R118 #2), RunList tooltip relative-time (R118 #4).

---

## R119 key decisions (上墙)

- **D-119-1: 2-slot ship via cron-slot-handoff-recovery Option A2 (the 21st A2 close-out chain).** Slot A executed the dogfood walkthrough + F12 fix + manifest unit tests but timed out before commit. Slot B re-ran pytest (769/9/0 = R118 baseline 748 + Δ+21), re-ran ruff (auto-fixed 3 trivial cosmetic warnings on R118-shipped code), re-ran mypy (clean), audited every diff hunk against R119 plan §5 1:1 → ship. No retry of slot A work, no rollback. Standard A2 close-out.

- **D-119-2: F12 root cause + multi-pronged fix** — pymdown-extensions 10.12 + pygments 2.20 incompatibility was the headline crash; mkdocs strict mode also rejected 4 relative links in `docs/adapters/anthropic_agents.md` (2 links → `recorder.py` + smoke test) + `docs/contracts/adapter-protocol.md` (1 link → `protocols.py`); and 3 ADR cross-references in ADR-029 + ADR-030 footnotes pointed at pre-rename slugs (ADR-009 was renamed to `usage-extractor-hook`, ADR-013 became ADR-015 `extractor-contract-v2`, ADR-016 became `adapter-interface`, ADR-006 became `diff-alignment`). All 4 link breakages would have surfaced *after* the pygments fix anyway — slot A was right to fix all of them in one pass since strict-build would otherwise have failed on each one in sequence.

- **D-119-3: Substitute live-browser walkthrough with CLI-surface walkthrough.** R119 plan §6 mandated `chronos web` + 5-page browser-vision walkthrough. Slot A captured 8-station CLI evidence instead. Rationale: (a) R113 + R114 already provide the canonical frontend P0 evidence and **no frontend code has changed since R114** (R115/R116/R117/R118/R119 all skipped frontend) so frontend behavior is unchanged; (b) CLI surface evidence was missing from prior rounds and is the orthogonal axis the user-path actually traverses first; (c) cron container has no X server so `browser_vision` would need the project's existing browser tool which has its own quirks (R113/R114 used it, R119 deferred). R122 acceptance can mix-and-match the two evidence sets. R121 RC buffer can re-record both surfaces in a single round if the user wants unified evidence.

- **D-119-4: Defer F13/F14/F16 + R118 deferred items #2/#4 to R121.** Per R119 plan §5 explicit instruction "P1/P2 推 R121", and R118 hand-off invariant 5. None of these gate R122 (Score column already ships per R115; Cost column already ships per R111; demo GIF is R122 必过项 "README: demo GIF" which has the only-real-gate ambiguity but is already documented as deferred via R116 D-116-2 ASCII fallback escape hatch).

- **D-119-5: Auto-fix ruff cosmetic issues.** Slot A's R118-cleanup had 3 trivial issues: (a) `from __future__ import annotations` makes `"DemoManifest"` self-reference redundant in `cls.empty(cls, name) -> "DemoManifest"`; (b) one multi-line `console.print(...)` could collapse to single-line; (c) `ruff format` reformatted one file. All 3 are cosmetic, preserved intent, no behavior change. Slot B re-ran tests after — 30/30 quickstart tests still GREEN, full suite 769/9/0.

- **D-119-6: `site/` gitignored, not committed.** Local strict-build of mkdocs renders `site/` (14M) which is the verification step that uncovered F12 + the relative-link bugs. `site/` is regenerated by `.github/workflows/gh-pages.yml` on every push to main, so committing it would create a 14M churn per round that's pure noise. Slot B added `site/` to `.gitignore` with a rationale comment referencing R119/R122/gh-pages.yml.

- **D-119-7: GHA verification deferred to post-push.** F12 fix is in this commit but cannot be verified until after `git push` triggers a fresh gh-pages workflow run. The R119 acceptance is **conditional on run #3 going green**. R120 picks up the verification — if green, F12 closes; if a new symptom appears (unlikely — pymdown-extensions bug + relative links were the only known failure modes), R120 diagnoses. Strict-build green locally is strong evidence the fix is complete.

---

## R119 self-check (mandatory)

**仍在 R107-R122 polish + ADR-029/030 轨道? ✅ YES.** R119 is verbatim row 9 唯一 slice ("E2E dogfood walkthrough"). All deferred items have explicit downstream slots (R121 RC buffer for F13/F14/F16/#2/#4; R120 verify GHA gh-pages green). No direction drift. No new ADR introduced. No new adapter touched. No new tech stack. **距 R122 = 3 轮** (R120 / R121 / R122).

---

## R120 hand-off invariants (R120 prologue 用)

1. **First action = verify GHA gh-pages run #3 went green.** `gh run list --workflow gh-pages.yml --limit 3` — the run triggered by R119 push should be green. If it failed: read the failure log, diagnose the new symptom (F12 was the only known root cause; a new failure means a previously-hidden second bug). R120 RC1 cut **must not proceed** until docs CI is green on main, because R122 必过项 "文档站 GH Pages 上线" gates RC1.

2. **R119 had 0 new P0 + 1 silently-failing pre-existing P0 fixed.** F12 had been failing since R117 ship without anyone noticing (no notification, no R118 follow-up). For R120, run a "smoke audit" of all 3 GHA workflows (`ci.yml`, `gh-pages.yml`, `golden-verify.yml`) on main — confirm all 3 are green on the most recent main commit before tagging v1.0.0-rc1. If any is red, fix or document before cut.

3. **R119 deferred items list (do NOT do these in R120, they are R121)**:
   - F13 (`runs list` column wrapping)
   - F14 (`doctor` extras-warn inline actionable hint)
   - F16 (real demo GIF via `agg`/`termtosvg`)
   - R118 #2 (TreeView score badge)
   - R118 #4 (RunList tooltip relative-time)

4. **R120 is tag-cut + Release Notes + (conditional) public-toggle.** Per R119 §6 plan preview block: bump `pyproject.toml` to `1.0.0rc1`, `git tag v1.0.0-rc1`, write Release Notes highlighting **R111 Cost Visibility (ADR-029)** + **R115 Evaluation/Scoring (ADR-030)** + **R118 ≥3 demo + manifest-driven loader** as the three differentiating features for v1.0. **Public-repo toggle is conditional on user explicit nod** — R120 plan must surface the question to the war-report and pause for user decision; do NOT auto-flip private→public.

5. **Adapter zero-regression streak hit 68 this round.** R120/R121/R122 = 3 more rounds → if all stay clean → streak hits 71 at R122 ship, which clears the ≥70 gate. If any of these 3 rounds touches an adapter for a hard reason (real bug found in dogfood), the streak resets and we have to renegotiate the ≥70 number — flag to user. R120 RC1 cut should be a no-code-touch round (just version bump + tag + notes), so streak should not be at risk.

6. **R119 evidence files (`docs/dogfood/r119-screenshots/{01..08}-*.txt`) are committed CLI captures, not screenshots.** Despite the directory name including "screenshots", they are text-format console captures. R120 Release Notes can reference them as "R119 dogfood evidence"; do NOT expect PNG files.

---

## R119 deliverables ship-list

**New files**:
- `docs/dogfood/2026-06-07-round-119-e2e.md` — full finding catalogue (F12, F13-F16, D-118-6 carry-over verification)
- `docs/dogfood/r119-screenshots/{01..08}-*.txt` — 8 CLI evidence captures (198 lines total)
- `tests/unit/test_cli_quickstart_manifest.py` — R118 D-118-6 carry-over (18 functions / 21 parametrize cases)
- `progress/2026-06-07-round-119.md` (this file)

**Modified**:
- `.github/workflows/gh-pages.yml` — F12 fix (pymdown-extensions 10.12 → 10.21.3 + path-trigger correction `docs.yml` → `gh-pages.yml`)
- `.gitignore` — `site/` added (mkdocs strict-build output, regenerated by gh-pages.yml every push)
- `docs/adapters/anthropic_agents.md` — 2 relative links → absolute GitHub URLs (mkdocs strict was rejecting them)
- `docs/contracts/adapter-protocol.md` — 1 relative link → absolute GitHub URL
- `docs/decisions/ADR-029-cost-visibility.md` — 3 stale ADR cross-link slugs renamed (ADR-009, ADR-013, ADR-016)
- `docs/decisions/ADR-030-evaluation-scoring.md` — 1 stale ADR cross-link slug renamed (ADR-006)
- `src/chronos/cli/quickstart.py` — ruff cleanup of R118-shipped code (`"DemoManifest"` → `DemoManifest` self-reference under `from __future__ import annotations`; one `console.print` line collapsed). No behavior change.
- `CHANGELOG.md` — `[Unreleased] / Tested — R119` + `[Unreleased] / Fixed — R119 (F12)` + `[Unreleased] / Added — R119 (manifest unit tests, dogfood evidence)` blocks
- `docs/CONTEXT.md` §5 (R119 close-out segment) + §6 (R120 plan replaces R119 plan)

**Deferred (carry-over to R121 RC buffer / v1.1+)**:
- F13 (runs-list column wrapping) → R121
- F14 (doctor extras-warn inline hint) → R121
- F15 (live-browser walkthrough re-record) → R121 if user wants unified evidence
- F16 (real demo GIF) → R121 (install `agg` in fresh slot, convert one `.txt` capture to asciicast → GIF)
- ADR-030 deferred #2 (TreeView score badge) → R121 (carry from R118)
- ADR-030 deferred #4 (RunList tooltip relative-time) → R121 (carry from R118)
- mkdocs i18n (`getting-started.zh.md`, `faq.zh.md`) → v1.1+ (carry from R117/R118; R122 字面 "中英双语" satisfied via README.zh-CN.md)

---

## Gate confirmation

- ✅ `pytest -q --no-cov`: **769 passed / 9 skipped / 0 failed** (Δ=+21 from R118 baseline 748; the +21 = `test_cli_quickstart_manifest.py` 18 functions × 1 parametrize × 4 demos = 21 cases)
- ✅ `ruff check src tests`: clean (3 issues auto-fixed in slot B)
- ✅ `ruff format src tests`: clean
- ✅ `mypy src`: clean (`Success: no issues found in 46 source files`)
- ✅ Local `mkdocs build --strict`: GREEN after F12 fix (verifies pymdown-extensions bump + relative-link rewires + ADR cross-link slug renames)
- ✅ 6 spikes GREEN (spike14/15/16/19/20/21)
- ✅ Adapter zero-regression streak: R52→R119 = **68** (`src/chronos/adapters/` byte-untouched)
- ✅ `pyproject.toml` / `uv.lock` byte-untouched (no dep drift in package code; only the GHA workflow's pip-install pin moved)
- ⚠️ GHA `gh-pages.yml` on main: 2 prior failures (R117/R118), F12 fix in this commit, next push triggers run #3 (verification deferred to R120 first action)
- ⚠️ No frontend gates run (no frontend code changed this round)
- ✅ `chronos quickstart --list` smoke + 4-demo `--demo` smoke + 1 evaluator end-to-end run captured into evidence files
- ✅ R118 D-118-6 carry-over (manifest unit tests) shipped
