name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        # All optional extras required: tests/spikes/integration import
        # langgraph, fastapi, autogen, crewai, claude-agent-sdk at module
        # level. `--extra dev` alone caused collection ImportErrors —
        # silently failing since R111. Fixed in R120 RC1 cut.
        run: uv sync --all-extras

      - name: Ruff lint
        run: uv run ruff check .

      - name: Ruff format check
        run: uv run ruff format --check .

      - name: Mypy
        run: uv run mypy src/chronos
        continue-on-error: true   # strict mypy — don't block until code matures

      - name: Pytest
        run: uv run pytest -v

      - name: Dogfood (examples run + suggested commands execute + docstring drift check)
        if: matrix.python-version == '3.11'
        run: bash scripts/dogfood.sh

      - name: Upload coverage
        if: matrix.python-version == '3.11'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: .coverage
          if-no-files-found: ignore
